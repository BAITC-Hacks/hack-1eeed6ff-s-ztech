# Передача Neverlose для подачи

Проверенное приложение опубликовано в официальном репозитории. **Отправка решения через платформу не подтверждена.** Её контролирует капитан Мейрам; дедлайн — 23 сентября 2026, 18:00 Астана (UTC+5). Git push не является отправкой формы.

## Сведения о проекте

- Команда и продукт: Neverlose.
- Кейс: Finance / Freedom — «Граф денег».
- Репозиторий: https://github.com/BAITC-Hacks/hack-1eeed6ff-s-ztech, ветка `main`.
- Мейрам: данные, алгоритмы, backend/API/CSV, README и интеграция.
- Алишер: интерфейс, граф, карточки, браузерные проверки, архитектура и демо.
- Точный SHA полученной версии: `git rev-parse HEAD`. Для фиксации версии используйте полный SHA коммита, выбранного перед отправкой.

Краткое описание для формы:

> Neverlose — локальное рабочее место AML-аналитика. Из исходных банковских parquet оно строит направленный граф, объяснимые гипотезы шести ролей, кластеры и очередь проверки. Любой gid связан с исходными переводами, условиями правил, альтернативной гипотезой и ограничениями наблюдения. Все 2248 узлов сохранены; UI, API и три обязательных CSV используют один расчёт. Основной сценарий работает без внешней модели, GPU, ключей и подписки.

## Запуск экспертом

Нужен установленный Python 3.12. Доступ к приватному репозиторию предоставляет организатор; runtime личного аккаунта участника не требует.

```bash
git clone https://github.com/BAITC-Hacks/hack-1eeed6ff-s-ztech.git
cd hack-1eeed6ff-s-ztech
python3.12 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.lock
python run.py
```

При настроенном SSH можно использовать `git clone git@github.com:BAITC-Hacks/hack-1eeed6ff-s-ztech.git`. Открыть `http://127.0.0.1:8000`; при занятом порте — `python run.py --port 8001`. Остановка — Ctrl+C. Node/npm не нужны для просмотра включённого `web/dist`.

## Артефакты и доказательства

- [nodes_roles.csv](../results/nodes_roles.csv): 2248 узлов, непустое evidence до 200 символов.
- [clusters.csv](../results/clusters.csv): 89 кластеров, все 19 изолятов сохранены.
- [top_nodes.csv](../results/top_nodes.csv): 50 узлов вместо обязательного минимума 20.
- [manifest](../results/manifest.json), [raw источники и hashes](hackalem/sources/manifest.json).
- [README](../README.md): установка, команды, формулы, пороги, диагностика, ограничения и масштабирование.
- [Архитектура](architecture.md), [демо до пяти минут](demo.md), [полные фактические проверки](validation.md).
- [Сторонние компоненты и AI-инструменты](../THIRD_PARTY.md); оригинальный starter раскрыт отдельно от кода команды.

Обязательное приложение проверено на двух ноутбуках из новых clone; подробные исторические SHA/условия сохранены в validation. Затем закрыто усиленное ревью backend (алгоритм 1.0.1), интегрирован B4 и добавлен необязательный F13. Текущий backend: 130 tests, lint/compileall; B4: 18 unit/13 contract E2E/3 real integration. После F13 повторены 3 сквозных browser сценария с запретом внешней сети сервера/Chromium; raw→CSV 0.936258 s. Все CSV по-прежнему совпадают побайтово. F13 отдельно проверен четырьмя настоящими вопросами OpenAI; UI агента ещё отсутствует.

## Границы версии

Роли — структурные гипотезы; score не является вероятностью виновности. Неполные входы, depth=4, порог 5000 KZT, июль и один банк видны в UI. 97 одинаковых строк сохранены; accuracy без ground truth не заявляется.

E1 — проверенный API структурной симуляции удаления 1–3 узлов; панель E1 в интерфейсе пока отсутствует и не демонстрируется как готовая функция. E2 не вошёл. F13 доступен через API/CLI при явном включении и наличии своего OpenAI-ключа, основной сценарий от него не зависит. Модель выбирает инструменты и проверенные утверждения; детерминированное ядро отдельно. Windows/Linux, Safari/Firefox и million-scale не проходили приёмку.

Техническая репетиция B заняла 141.218 s и подтверждает маршрут действий/выгрузки; устную речь человека она не измеряет. Команде нужно произнести сценарий с собственным таймером.

## Действия капитана

1. Убедиться, что на платформе выбран Finance и организатор видит официальный репозиторий.
2. Зафиксировать выбранный полный SHA, проверить наличие трёх CSV, README и локальной сборки.
3. Заполнить требуемые поля платформы, указать репозиторий/версию и отправить решение до 18:00.
4. Сохранить фактическое подтверждение и время отправки. Только после этого отмечать G6/подачу выполненными.

Отчёты часов 1–3 опубликованы по фактической работе. Час 4 содержит завершение и живую проверку F13, повтор offline workflow; отчёт опубликован по фактическому времени. Час 5 фиксируется только после 17:00 по реальным проверкам/подаче.
