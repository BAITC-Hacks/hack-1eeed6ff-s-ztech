# Состояние — 23 сентября 2026, 17:41 Астана

Проверенный код **e615483ba765fbf0a6288693d830301fd5a023e4**: общие прямые получатели 2–5 счетов, GPT-6 Astra по умолчанию и интегрированный аудит Алишера B7 `0b6a20e` (merge `9e3dcf9`). Изменения публикуются в официальные main/analysis-core с сохранением истории. Фактическое состояние remote проверяется через git fetch/ls-remote.

Все пять обязательных функций Freedom сохранены: воспроизводимый pipeline, роли всех 2248 узлов, формальные объяснимые правила, 89 кластеров, top50, направленный граф и поиск. Алгоритм 1.0.2 и run_id `d194e7cad44fdc0265a5d8f2eeeb9ab7a4a16a88a98e3eeecd164ad01a4658d3` не менялись. Три CSV побайтно прежние.

Новая функция ищет прямых получателей от всех выбранных отправителей. UI работает без API-ключа: точная группа → получатели → вклад каждого и source_refs → карточка → подготовка вопроса AI. Группа сохраняется при закрытии панели. Пользователь явно разрешил этот ограниченный этап после 17:15; после его выпуска — только подтверждённые P0/P1.

Проверки: **209 Python, 43 frontend unit, 62 browser contract, 5 real offline integration — PASS**. Отдельно выполнены семь проверок нового offline UI, обе темы и размеры 1440/1280/1024. Build и scoped ruff — PASS. Четыре новых live-вопроса: 8 Responses-вызовов, 17013 входных / 422 выходных токена; последний вопрос из браузера занял 7.239 s, источники вернули HTTP 200, Markdown реально скачан. [Полный отчёт](evidence/common-a/report.json). Это ограниченная приёмка MVP, не размеченный benchmark точности или банковский production.

Из B7 сохранены защита Host, дополнительная сверка метаданных/кластеров и исправления фокуса/контекста UI. Независимые review и найденные исправления отражены в A/validation. Последний clean clone до этого этапа принят на `f45632f`; новая проверка публикации фиксируется отдельно.

`.env` существует только локально. В GitHub — `.env.example` с пустым ключом; жюри ставят свой по README. Ключ участника не нужен основному режиму и поиску общих получателей. Часы 1–5 уже отражены в фактических отчётах.

Ограничения: UI E1 отсутствует; E2, маршруты и временные фильтры не реализованы. У 70 связанных кластеров консервативный шаблон гипотезы, 19 изолятов описаны отдельно. Подача — GitHub и кнопка **«Сдать проект»** до 18:00; нажатие капитаном ещё не подтверждено. После 18:00 оцениваемую версию не менять.
